<Page x:Class="HeavenUsurperToolkit.KeyConverter"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HeavenUsurperToolkit"
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="800"
      Title="KeyConverter">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Rectangle Grid.Row="0" Grid.ColumnSpan="2" Height="65"/>
        <Rectangle Grid.Row="6" Grid.ColumnSpan="2" Fill="#FFCFAEFD" Margin="0,20,0,0" Drop="Rectangle_Drop" AllowDrop="True"/>

        <Button x:Name="BackButton" Content="←" Height="45" Width="45" Margin="10,292,745,10" 
            Grid.Row="6" Grid.Column="0" Style="{StaticResource ModernButton}" 
            FontSize="30" Click="BackButton_Click" Grid.ColumnSpan="2" VerticalAlignment="Bottom" HorizontalAlignment="Left">
        </Button>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="10">
            <TextBlock Text="Keys:" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
            <Slider Style="{StaticResource ModernSlider}" Maximum="10" Minimum="2" Value="8" 
                IsSnapToTickEnabled="True" TickFrequency="1" x:Name="KeyValue"/>
            <TextBlock Text="{Binding Value, ElementName=KeyValue, StringFormat='Key: {0}'}" 
                FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="10">
            <TextBlock Text="Gaps:" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
            <Slider Style="{StaticResource ModernSlider}" Maximum="20" Minimum="0" Value="10" 
                IsSnapToTickEnabled="True" TickFrequency="1" x:Name="GapValue"/>
            <TextBlock Text="{Binding Value, ElementName=GapValue, StringFormat='Gap: {0}'}" 
                FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="0" Margin="10,10,10,10">
            <TextBlock Text="Clean:" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
            <Slider Style="{StaticResource ModernSlider}" Maximum="16" Minimum="0" Value="4" 
                IsSnapToTickEnabled="True" TickFrequency="1" x:Name="CleanValue"/>
            <TextBlock Text="{Binding Value, ElementName=CleanValue, StringFormat='Clean: {0}'}" 
                FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="1" Margin="10,10,10,10">
            <TextBlock Text="Blank:" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
            <Slider Style="{StaticResource ModernSlider}" Maximum="9" Minimum="0" Value="0" 
                IsSnapToTickEnabled="True" TickFrequency="1" x:Name="BlankValue"/>
            <TextBlock Text="{Binding Value, ElementName=BlankValue, StringFormat=Blank: \{0\}}" 
                FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Margin="10">
            <TextBlock Text="Seed:" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
            <TextBox x:Name="SeedText" Style="{StaticResource ModernTextBox}" Text="114514"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="3" Margin="10">
            <CheckBox x:Name="CreatorCheckBox" Content="Creator" Style="{StaticResource ModernCheckBox}" 
                FontSize="18" HorizontalAlignment="Left"/>
            <TextBox x:Name="CreatorText" Style="{StaticResource ModernTextBox}" Margin="0,5,0,0"/>
        </StackPanel>

        <Button x:Name="InstructionButton" Content="Instruction" Style="{StaticResource ModernButton}" 
            Click="InstructionButton_Click" Grid.Row="6" Grid.Column="1" Margin="0,0,10,10" Height="45" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="130"/>

        <StackPanel x:Name="ProgressStackPanel" Grid.Row="6" Grid.ColumnSpan="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,60">
            <ProgressBar x:Name="ConversionProgress" Minimum="0" Maximum="100" Height="20" Width="400" Margin="0,0,0,5" Style="{StaticResource ModernProgressBar}"/>
            <TextBlock x:Name="ProgressText" Text="0/0" HorizontalAlignment="Center" FontSize="20" Foreground="#FF8E6CDC" FontWeight="Bold"/>
        </StackPanel>

        <TextBlock Text="Drop Folder or .osu File" Grid.Row="6" Grid.ColumnSpan="2" VerticalAlignment="Center" 
            HorizontalAlignment="Center" FontSize="24" Foreground="#FF8E6CDC" FontWeight="Bold" AllowDrop="True" Drop="TextBlock_Drop"/>
        <WrapPanel Grid.Row="6" Grid.ColumnSpan="2" Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Center" Width="780" ItemWidth="75" ItemHeight="30" Height="30" Margin="0,30,0,0">
            <CheckBox x:Name="C1k" Content="1k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C2k" Content="2k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C3k" Content="3k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C4k" Content="4k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C5k" Content="5k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C6k" Content="6k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C7k" Content="7k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C8k" Content="8k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="C9k" Content="9k" Style="{StaticResource ModernCheckBox}"/>
            <CheckBox x:Name="AnotherAlgorithm" Content="Another" Style="{StaticResource ModernCheckBox}" Margin="0,0,-20,0"/>
        </WrapPanel>
    </Grid>
</Page>
