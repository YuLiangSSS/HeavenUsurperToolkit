<Page x:Class="HeavenUsurperToolkit.PatternGenerator"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HeavenUsurperToolkit"
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="1281"
      Title="PatternGenerator">

    <Grid>
        <Rectangle Height="260" VerticalAlignment="Bottom" Fill="#FFCFAEFD" Margin="0,0,0,0" AllowDrop="True" Drop="Rectangle_Drop"/>
        <TextBlock Text="Drop .osu File" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,130" FontSize="24" FontWeight="Bold" Foreground="#FF8E6CDC"/>
        <Button x:Name="BackButton" Content="←" Height="45" Width="45" Margin="10,0,0,10" 
            HorizontalAlignment="Left" VerticalAlignment="Bottom" Style="{StaticResource ModernButton}" 
            FontSize="30" Click="BackButton_Click" />
        <Rectangle Height="65" VerticalAlignment="Top"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="10,70,10,269">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="BPM" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="BPMText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Divide" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="DivideText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="StartTime" VerticalAlignment="Center" Margin="0,0,5,5" Foreground="#FF8E6CDC" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="StartTimeText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="EndTime" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="EndTimeText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Note" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="1" x:Name="NoteText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Gap" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="5" Grid.Column="1" x:Name="GapText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>
                </Grid>
                <Grid Height="65">
                    <CheckBox x:Name="RandomDistribution" Content="Random Distribution" HorizontalAlignment="Left" Style="{StaticResource ModernCheckBox}" FontSize="18" FontWeight="Bold"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Max" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="MaxText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Average" VerticalAlignment="Center" Margin="0,0,5,5" Foreground="#FF8E6CDC" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="AverageText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Line" VerticalAlignment="Center" Margin="0,0,5,5" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" HorizontalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="LineText" Margin="0,0,0,5" Style="{StaticResource ModernTextBox}"/>
                </Grid>
                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Grid.Column="0" x:Name="WithDensity" Content="With Density" Style="{StaticResource ModernCheckBox}" FontSize="18" FontWeight="Bold" Height="65" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Grid.Column="1" x:Name="AddProcessButton" Content="Add" Style="{StaticResource ModernButton}" Click="AddProcessButton_Click" VerticalAlignment="Center" Height="45" Margin="369,0,0,0"/>
                    <Button Grid.Column="1" x:Name="ManualButton" Content="Manual" Style="{StaticResource ModernButton}" VerticalAlignment="Center" Height="45" HorizontalAlignment="Left" Margin="269,0,0,0" Click="ManualButton_Click" Width="95"/>
                    <Button Grid.Column="1" x:Name="ProcessButton" Content="Process" Style="{StaticResource ModernButton}" VerticalAlignment="Center" Height="45" HorizontalAlignment="Left" Margin="169,0,0,0" Click="ProcessButton_Click" Width="95"/>
                </Grid>
            </StackPanel>
            <DataGrid Grid.Column="1" x:Name="SequenceDataGrid" Style="{StaticResource ModernDataGrid}" Margin="10,139,10,266" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="BPM" Binding="{Binding BPM}" Width="45"/>
                    <DataGridTextColumn Header="Divide" Binding="{Binding Divide}" Width="56"/>
                    <DataGridTextColumn Header="StartTime" Binding="{Binding StartTime}" Width="77"/>
                    <DataGridTextColumn Header="EndTime" Binding="{Binding EndTime}" Width="70"/>
                    <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="47"/>
                    <DataGridTextColumn Header="Gap" Binding="{Binding Gap}" Width="41"/>
                    <DataGridCheckBoxColumn Header="Random" Binding="{Binding RandomDistribution}" Width="67"/>
                    <DataGridTextColumn Header="Max" Binding="{Binding Max}" Width="44"/>
                    <DataGridTextColumn Header="Average" Binding="{Binding Average}" Width="67"/>
                    <DataGridTextColumn Header="Line" Binding="{Binding Line}" Width="42"/>
                    <DataGridCheckBoxColumn Header="Density" Binding="{Binding WithDensity}" Width="64"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Column="1" Margin="10,70,10,0" VerticalAlignment="Top">
                <TextBlock Text="Output File Path" FontSize="18" FontWeight="Bold" Foreground="#FF8E6CDC" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="OutputFilePathText" Grid.Column="0" Style="{StaticResource ModernTextBox}" Margin="0,0,5,0"/>
                    <Button x:Name="BrowseFileButton" Grid.Column="1" Content="Browse" Style="{StaticResource ModernButton}" Click="BrowseFileButton_Click" Width="90"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
