<Page x:Class="HeavenUsurperToolkit.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HeavenUsurperToolkit"
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="800"
      Title="MainPage" Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 主页面内容 -->
        <StackPanel Margin="0,80,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Height="760" Width="171" FlowDirection="LeftToRight">

            <!-- 导航按钮 -->
            <Button x:Name="TestButton" 
                Content="Test"
                Width="120" 
                Height="40"
                Margin="0,20,0,0"
                Click="Test_Click"
                Visibility="Collapsed"
                Style="{StaticResource ModernButton}" />

            <!-- 导航按钮 -->
            <Button x:Name="BtnToLN" 
                Content="LN"
                Width="120" 
                Height="40"
                Click="BtnToLN_Click"
                Margin="0,20,0,0"
                Style="{StaticResource ModernButton}" />
            <Button x:Name="BtnToKeyConverter" 
                Content="Key"
                Width="120" 
                Height="40"
                Margin="0,20,0,0"
                Click="BtnToKeyConverter_Click"
                Style="{StaticResource ModernButton}" />
            <Button x:Name="BtnToLevel" 
                Content="Level"
                Width="120" 
                Height="40"
                Margin="0,20,0,0"
                Click="BtnToLevel_Click"
                Style="{StaticResource ModernButton}" />

        </StackPanel>

        <!-- 导航按钮 -->
    </Grid>
</Page>