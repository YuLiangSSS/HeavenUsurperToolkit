<Window x:Class="HeavenUsurperToolkit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HeavenUsurperToolkit"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="HeavenUsurper's Toolkit" Height="850" Width="800" 
        WindowStyle="None" 
        Background="#FFD1BFEA"
        PreviewMouseLeftButtonDown="MainWindow_PreviewMouseLeftButtonDown" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome 
            GlassFrameThickness="0" 
            CornerRadius="0" 
            CaptionHeight="0" 
            ResizeBorderThickness="5" />
    </shell:WindowChrome.WindowChrome>
    <Grid x:Name="ContentArea" ClipToBounds="True">
        <Canvas x:Name="ParticleCanvas" IsHitTestVisible="False" Panel.ZIndex="999"/>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            
        <!-- 添加标题栏背景 -->
        <Rectangle Height="65" VerticalAlignment="Top" Fill="#FFB9A6D2"/>
        
        <!-- 窗口控制按钮 -->    
        <Button x:Name="CloseButton" Content="X" Height="45" Width="45" Margin="0,10,10,0" 
                HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource ModernButton}" 
                FontSize="25" Click="CloseButton_Click" />
                
        <Button x:Name="MaximizeButton" Content="□" Height="45" Width="45" Margin="0,10,60,0" 
                HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource ModernButton}" 
                FontSize="30" Click="MaximizeButton_Click" FontFamily="Cascadia Mono SemiBold" FontWeight="Bold" />

        <Button x:Name="MinimizeButton" Content="一" Height="45" Width="45" Margin="0,10,110,0" 
                HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource ModernButton}" 
                FontSize="25" Click="MinimizeButton_Click" />

        <!-- 窗口标题 HeavenUsurper's Toolkit -->
        <TextBlock Text="HeavenUsurper's Toolkit" FontSize="20" FontWeight="SemiBold"
                   VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20,20,0,0" Foreground="#FF8E6CDC"/>
    </Grid>
</Window>